# Pipeline Source Checksum

The local simulation source used for the milestone run is:

```text
scripts/a2t_simulate_pipeline.py
```

SHA-256:

```text
4320062ad70774f45bd742bac788ec5efaa504b158699f5295c2f6598712879a
```

The source file is available in the local publication bundle. The GitHub connector upload includes this checksum and the generated manifests. The full script can be added to the repository later through normal `git`/`gh` authentication if a complete executable mirror is required.
